<p id="notice"><%= notice %></p>
<p>
  <strong>Name:</strong>
  <%= @batch.name %><br>
</p>
<p>
  <strong>Channel Name:</strong>
  <% @batch.channels.each do |channel|  %>
  <li>
  	<%=link_to channel.name , channel_path(channel.id)%>
  </li>
  <% end -%>
</p>
<!-- <p>
  <strong>Student Names:</strong>

</p> -->
<% if user_signed_in? && current_user.is_admin %>
	<%= link_to 'Edit', edit_batch_path(@batch) %>
<% end -%>|
	<%= link_to 'Back', batches_path %>
